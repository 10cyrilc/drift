# serve

The `serve` command starts the DRIFT server.

## Usage

```bash
drift serve [flags]
```

## Flags

- `-p PORT`: Port to run the server on. This overrides the default port and the `DRIFT_PORT` environment variable.
