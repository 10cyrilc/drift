# release

The `release` command releases a reserved zrok token.

## Usage

```bash
drift release
```

When you run this command, you will be presented with a list of reserved tokens. You can select one or more tokens to release.
